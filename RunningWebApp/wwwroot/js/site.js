// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.


/*
document.addEventListener('DOMContentLoaded', () => {
    // Code that runs when the DOM is loaded and verifies we have attached event handlers
    console.log('DOM Loaded');
    // Locate the time buttons (list items)
    const historyRow = document.querySelectorAll('tr');
    historyRow.forEach((historyRow) => {
        // Attach an event handler for click on a list item
        historyRow.addEventListener('click', (event) => {
            console.log('history rows added');    
            highlightRow(event.currentTarget);
        });
    });
});

function highlightRow(row) {
    console.log('booyah');
    row.classList.add('shine');
}
*/